version: "2"

run:
  tests: false

linters:
  enable:
    - bodyclose
    - exhaustive
    - goconst
    - godot
    - gomoddirectives
    - goprintffuncname
    - gosec
    - misspell
    - nakedret
    - nestif
    - nilerr
    - noctx
    - nolintlint
    - prealloc
    - revive
    - rowserrcheck
    - sqlclosecheck
    - tparallel
    - unconvert
    - unparam
    - whitespace
    - wrapcheck
  settings:
    exhaustive:
      default-signifies-exhaustive: true
    misspell:
      # `enviroments` is spelled wrong on purpose in config.Parse's doc comment: it
      # is the example of the typo strict decoding exists to catch. Correcting it
      # would delete the point of the sentence.
      ignore-rules:
        - enviroments
    wrapcheck:
      # wrapcheck exists so an error from someone else's package arrives in brama's
      # words rather than theirs. brama's own packages already speak in brama's
      # words: `shim.Ensure` returns "installing the shim on web-1: ...", and the
      # caller that has more to say wraps it again. Treating every internal package
      # boundary as foreign would turn a real check into a ritual, and the ritual
      # would double-wrap the messages it touched.
      ignore-package-globs:
        - github.com/bramaos/brama/internal/*
  exclusions:
    generated: lax
    presets:
      - common-false-positives
    rules:
      - text: '(slog|log)\.\w+'
        linters:
          - noctx
      # The Result implementations in internal/cli are one-line methods satisfying
      # renderer.Result, which documents each of them on the interface. Repeating
      # that on all four methods of all three Results is noise, not documentation.
      - path: internal/cli/
        text: 'exported method .* should have comment'
        linters:
          - revive

issues:
  max-issues-per-linter: 0
  max-same-issues: 0

formatters:
  enable:
    - gofumpt
    - goimports
  exclusions:
    generated: lax
